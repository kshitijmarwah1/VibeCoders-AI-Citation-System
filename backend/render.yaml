services:
  - type: web
    name: ai-verification-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn services.api.main:app --host 0.0.0.0 --port 10000
    plan: free
